<%= form_for(current_user.relationships.find_by(child_id: @user),
             html: { method: :delete },
             remote: true) do |f| %>
	       <%= f.submit "Unparent", class: "btn btn-large" %>
<% end %>
