<%= form_for(current_user.relationships.build(child_id: @user.id),
remote: true) do |f| %>
  <div><%= f.hidden_field :child_id %></div>
    <%= f.submit "Parent", class: "btn btn-large btn-primary" %>
<% end %>
